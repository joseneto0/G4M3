#include <bits/stdc++.h>

using namespace std;

int n, m;
tuple<char, int, int, int> infos;
vector<int> root(n+1);
int find(int x){
    while()
}
void unionn(int a, int b){
    int x = find(a);
    int y = find(b);
    
}

int main(){
    ios_base::sync_with_stdio(0); cin.tie(NULL); cout.tie(NULL);
    cin >> n >> m;
    char i;
    int a, b, w;
    for (int i = 0; i <= n; i++){
        root[i] = i;
    }
    for (int _ = 0; _ < m; _++){
        cin >> i >> a >> b >> w;
        
    }
    return 0; 
}
